import React from 'react';

const EmailContact = () => {
    return (
        <React.Fragment>
            <div className="w-full h-[300px] text-center mt-10">
                email
            </div>
        </React.Fragment>
    );
}

export default React.memo(EmailContact);
